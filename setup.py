#!/usr/bin/env python
from setuptools import setup, find_packages

__version__ = '0.1'

setup(
    name='caddy',
    description='Urban freedom through convenient, affordable storage spread throughout the city.',
    version=__version__,
    packages=find_packages(),
)
